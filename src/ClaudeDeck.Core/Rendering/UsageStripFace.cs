namespace ClaudeDeck.Core.Rendering;

/// <summary>
/// What one usage window looks like on an encoder's touch strip.
/// </summary>
public sealed record UsageStripFace(string Title, string Value, int Indicator, string Colour);
